import { define } from "../utils.ts";
import SearchComponent from "../islands/SearchComponent.tsx";

export default define.page(function Search() {
  return (
    <>
      <SearchComponent />
    </>
  );
});
